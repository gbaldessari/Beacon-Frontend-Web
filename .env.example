# Beacon Frontend — variables de entorno (Vite)
# Local: copiar a .env. Vercel: definir en Project Settings → Environment Variables.

VITE_BACK_URL=http://localhost:3000
# Note: URL base del API Nest.
# Local: http://localhost:3000
# Producción (Vercel): https://<tu-servicio>.up.railway.app (URL pública del backend en Railway).

# Make sure to keep this file private and do not share it publicly.
